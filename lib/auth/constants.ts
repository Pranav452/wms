// Plain constants safe to import from client components (unlike password.ts,
// which is server-only because it pulls in bcrypt).
export const PASSWORD_HINT = 'At least 8 characters, with a letter and a number'
